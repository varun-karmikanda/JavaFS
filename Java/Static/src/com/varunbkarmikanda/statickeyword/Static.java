package com.varunbkarmikanda.statickeyword;

public class Static {
    // Primary usage: Memory management

    // Applied to variables, methods, blocks and nested classes

    // Basically attach to a particular class
    // Object of that class share that

    // Static method cannot use non-static data members or non-static methods directly

    // Cannot use "this" and "super" in static context

    // Static variables get initialized before an instance is created

    // When we have to attach something to the class and not object
}
